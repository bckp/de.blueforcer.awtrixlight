meta {
  name: 13 JSON right press
  type: http
  seq: 13
}

post {
  url: {{homey_base_url}}/api/app/de.blueforcer.awtrixlight/awtrixng/button/{{device_uid}}/{{callback_token}}
  body: json
  auth: none
}

body:json {
  {
    "button": "right",
    "state": 1,
    "uid": "{{device_uid}}"
  }
}

tests {
  test("Homey accepts a JSON right press", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.eql({ ok: true });
  });
}

docs {
  Expected result: HTTP 200 with {"ok":true} and exactly one run of the right-button Flow.
}
